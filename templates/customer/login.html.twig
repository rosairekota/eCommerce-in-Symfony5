{% extends "base.html.twig" %}

{% block body %}
<div class="container mt-5">
    <div class="row">

        <div class="col-md-6 img-thumbnail p-5 mr-2">
            {{form_start(formlogin)}}
            {{form_widget(formlogin)}}
            <center> <button type="submit" class="btn btn-success">Se connecter</button></center>
            {{form_end(formlogin)}}

        </div>
        <div class="col-md-4 img-thumbnail">

            <center> <a href="{{path('customer_registration')}}" class="btn btn-primary mt-4">Nouveau Client,
                    Inscrivez-vous !</a></center>
        </div>
    </div>
    {% endblock body %}